---
queue_state: in-progress
---
Read the existing code before editing, then implement the task summary and acceptance criteria using the repository's conventions.

Work inside the task worktree for the primary repo. Keep changes scoped to the task and preserve unrelated user or generated changes.

Append meaningful progress notes to the task Work Log as you go.
